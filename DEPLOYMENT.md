# 🚀 GitHub Actions Deployment Guide

## Настройка GitHub Pages

### 1. Включите GitHub Pages в настройках репозитория:

1. Перейдите в **Settings** вашего репозитория
2. В левой панели выберите **Pages**
3. В разделе **Source** выберите **GitHub Actions**
4. Нажмите **Save**

### 2. Workflow автоматически запустится при пуше в main/master ветку

Workflow выполнит следующие шаги:
- Установит зависимости с помощью `npm ci`
- Проверит типы с помощью `npm run check`
- Соберет проект с помощью `npm run build`
- Задеплоит результат в директорию `./build`

### 3. Мониторинг деплоя

Вы можете отслеживать прогресс деплоя во вкладке **Actions** вашего репозитория.

### 4. Доступ к сайту

После успешного деплоя ваш сайт будет доступен по адресу:
`https://ваш-логин.github.io/название-репозитория`

## Решение возможных проблем

### Если деплой не работает:

1. **Проверьте логи GitHub Actions** во вкладке Actions
2. **Убедитесь, что ветка называется main или master**
3. **Проверьте, что все зависимости установлены корректно**

### Если показывается только README.md:

Это обычно происходит когда:
- Используется неправильный адаптер (adapter-auto вместо adapter-static)
- Неправильно указана директория для публикации
- Не настроены правильные маршруты в SvelteKit

Наш workflow использует `@sveltejs/adapter-static` и публикует из директории `./build`.

## Локальная проверка

Перед пушем в репозиторий рекомендуется проверить сборку локально:

```bash
npm run build
npm run preview
```

Это позволит убедиться, что сайт собирается корректно и работает как ожидается.