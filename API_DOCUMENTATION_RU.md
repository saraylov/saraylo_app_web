# API Документация для Saraylo App Website

## Общая информация

Это RESTful API для управления контентом веб-сайта Saraylo App. API предоставляет функциональность для создания, чтения, обновления и удаления данных о продуктах, членах команды и элементах навигации.

### Базовый URL
```
https://ваш-сайт.ru/api/v1
```

### Формат данных
- Все запросы и ответы используют формат JSON
- Даты представлены в формате ISO 8601
- Кодировка UTF-8

### Стандартный формат ответа
```json
{
  "success": true,
  "data": {},
  "message": "Описание операции",
  "timestamp": "2024-01-15T10:30:00.000Z"
}
```

При ошибке:
```json
{
  "success": false,
  "error": "Тип ошибки",
  "message": "Описание ошибки",
  "details": {},
  "timestamp": "2024-01-15T10:30:00.000Z"
}
```

## Аутентификация

### POST /auth/login
Аутентификация пользователя и получение JWT токена

**Запрос:**
```json
{
  "email": "admin@example.com",
  "password": "admin123"
}
```

**Ответ успешный:**
```json
{
  "success": true,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": "1",
      "email": "admin@example.com",
      "role": "admin",
      "name": "Администратор"
    },
    "expiresIn": 86400
  },
  "message": "Успешная аутентификация"
}
```

**Коды ошибок:**
- 400: Неверные учетные данные
- 401: Неавторизованный доступ

## Продукты

### GET /products
Получение списка продуктов с пагинацией и поиском

**Параметры запроса:**
- `search` (опционально) - текст для поиска
- `page` (опционально, по умолчанию 1) - номер страницы
- `limit` (опционально, по умолчанию 10) - количество элементов на странице

**Пример запроса:**
```
GET /api/v1/products?page=1&limit=5&search=TaskFlow
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "data": [
      {
        "id": "taskflow-pro",
        "name": "TaskFlow Pro",
        "description": "Advanced task management application...",
        "type": "desktop",
        "icon": "📋",
        "images": ["/images/taskflow-1.jpg", "/images/taskflow-2.jpg"],
        "features": ["AI-powered task prioritization", "Real-time team collaboration"],
        "technologies": ["SvelteKit", "TypeScript", "SQLite", "Electron"],
        "link": "#",
        "releaseDate": "2024-03-15T00:00:00.000Z",
        "createdAt": "2024-01-15T10:00:00.000Z",
        "updatedAt": "2024-01-15T10:00:00.000Z"
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 5,
      "total": 25,
      "totalPages": 5,
      "hasNext": true,
      "hasPrev": false
    }
  },
  "message": "Продукты успешно получены"
}
```

### GET /products/{id}
Получение конкретного продукта по ID

**Пример запроса:**
```
GET /api/v1/products/taskflow-pro
```

### POST /products
Создание нового продукта *(требует аутентификации: admin/editor)*

**Запрос:**
```json
{
  "name": "Новый продукт",
  "description": "Описание продукта",
  "type": "desktop",
  "icon": "📱",
  "images": ["/images/new-product-1.jpg"],
  "features": ["Функция 1", "Функция 2"],
  "technologies": ["React", "Node.js"],
  "link": "https://example.com",
  "releaseDate": "2024-12-01T00:00:00.000Z"
}
```

### PUT /products/{id}
Обновление продукта *(требует аутентификации: admin/editor)*

### DELETE /products/{id}
Удаление продукта *(требует аутентификации: admin)*

## Члены команды

### GET /team-members
Получение списка членов команды

**Параметры запроса:**
- `page` (опционально, по умолчанию 1)
- `limit` (опционально, по умолчанию 10)

### GET /team-members/{id}
Получение конкретного члена команды

### POST /team-members
Создание члена команды *(требует аутентификации: admin/editor)*

**Запрос:**
```json
{
  "name": "Иван Петров",
  "role": "Frontend Developer",
  "bio": "Опытный разработчик с 5-летним стажем",
  "skills": ["JavaScript", "React", "Vue.js"],
  "avatar": "/images/team/ivan.jpg"
}
```

### PUT /team-members/{id}
Обновление члена команды *(требует аутентификации: admin/editor)*

### DELETE /team-members/{id}
Удаление члена команды *(требует аутентификации: admin)*

## Навигация

### GET /navigation
Получение элементов навигации

**Параметры запроса:**
- `showAll` (опционально) - показать все элементы (для админов)
- `page` (опционально)
- `limit` (опционально)

**Пример для публичного API:**
```
GET /api/v1/navigation
```

**Пример для админов:**
```
GET /api/v1/navigation?showAll=true&page=1&limit=10
```

### POST /navigation
Создание элемента навигации *(требует аутентификации: admin/editor)*

**Запрос:**
```json
{
  "label": "Новый раздел",
  "path": "/new-section",
  "icon": "⭐",
  "order": 5,
  "isActive": true
}
```

### PUT /navigation/{id}
Обновление элемента навигации *(требует аутентификации: admin/editor)*

### DELETE /navigation/{id}
Удаление элемента навигации *(требует аутентификации: admin)*

## Коды состояния HTTP

| Код | Описание |
|-----|----------|
| 200 | Успешный запрос |
| 201 | Ресурс успешно создан |
| 400 | Неверный запрос или валидация не пройдена |
| 401 | Неавторизованный доступ |
| 403 | Доступ запрещен |
| 404 | Ресурс не найден |
| 500 | Внутренняя ошибка сервера |

## Права доступа

| Роль | Разрешения |
|------|------------|
| `admin` | Полный доступ ко всем операциям |
| `editor` | Создание и редактирование контента |
| `viewer` | Только чтение (не реализовано в текущем API) |

## Примеры использования

### JavaScript/TypeScript клиент
```javascript
// Аутентификация
async function login(email, password) {
  const response = await fetch('/api/v1/auth/login', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ email, password })
  });
  
  return await response.json();
}

// Получение списка продуктов
async function getProducts(page = 1, limit = 10, search = '') {
  const params = new URLSearchParams({
    page: page.toString(),
    limit: limit.toString()
  });
  
  if (search) {
    params.append('search', search);
  }
  
  const response = await fetch(`/api/v1/products?${params}`);
  return await response.json();
}

// Создание продукта
async function createProduct(productData, token) {
  const response = await fetch('/api/v1/products', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`
    },
    body: JSON.stringify(productData)
  });
  
  return await response.json();
}
```

### Python клиент
```python
import requests
import json

class SarayloApiClient:
    def __init__(self, base_url):
        self.base_url = base_url
        self.token = None
    
    def login(self, email, password):
        response = requests.post(
            f"{self.base_url}/auth/login",
            json={"email": email, "password": password}
        )
        data = response.json()
        if data["success"]:
            self.token = data["data"]["token"]
        return data
    
    def get_products(self, page=1, limit=10, search=None):
        params = {"page": page, "limit": limit}
        if search:
            params["search"] = search
            
        response = requests.get(f"{self.base_url}/products", params=params)
        return response.json()
    
    def create_product(self, product_data):
        if not self.token:
            raise Exception("Необходима аутентификация")
            
        response = requests.post(
            f"{self.base_url}/products",
            headers={
                "Authorization": f"Bearer {self.token}",
                "Content-Type": "application/json"
            },
            json=product_data
        )
        return response.json()
```

## Безопасность

### Рекомендации по безопасности:
1. Используйте HTTPS в production
2. Храните токены в безопасном месте (HttpOnly cookies)
3. Реализуйте rate limiting для предотвращения злоупотреблений
4. Регулярно меняйте секретные ключи
5. Логируйте важные события безопасности

### В production необходимо:
- Заменить моковые данные пользователей на реальную систему аутентификации
- Использовать настоящую базу данных вместо in-memory хранилища
- Реализовать полноценную JWT библиотеку
- Добавить валидацию входных данных на уровне схем
- Настроить CORS правильно
- Реализовать логирование и мониторинг

## Ограничения текущей реализации

1. Данные хранятся в памяти (при перезапуске сервера они теряются)
2. Используется упрощенная система токенов вместо полноценного JWT
3. Моковые пользователи вместо реальной системы регистрации
4. Нет автоматического rate limiting
5. Нет продвинутой валидации данных

Для production рекомендуется заменить эти компоненты на более надежные решения.